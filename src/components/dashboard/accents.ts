import { AccentName, AccentTokens } from "@/types";

// Shared color tokens for KPI cards / chart panel accents across the dashboard.
export const ACCENTS: Record<AccentName, AccentTokens> = {
  blue: { bg: "#EAF2FC", fg: "#1C69D4" },
  teal: { bg: "#E8FAF7", fg: "#16B8A6" },
  indigo: { bg: "#EEF0FE", fg: "#5B6EF5" },
  sky: { bg: "#EAF6FE", fg: "#4FA8E8" },
};
